sudo: false

language: python

python:
   - "3.5"

before_install:
   - pip install --upgrade pip
   - psql -c 'create database test;' -U postgres

addons:
   postgresql: "9.6"

install: true

script:
   - make
   - nosetests tests\tests.py

env:
   - SQE_IDB_PGDB_TABLE="test"

services:
   - postgresql